/* eslint-disable no-console  */

import React from 'react';

import ClickWrapper from '../index';
import mockProps from '../schema';

const Default = (): JSX.Element => <ClickWrapper {...mockProps} />;

export default Default;
