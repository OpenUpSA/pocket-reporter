---
name: Card
menu: Components
---


import { PropsTable } from 'docz';
import Card from './index';


# Card

**⚠️ In order to view or edit this component run `yarn start:storybook` to open it in a sandbox environment.**

## `index.jsx`

**A presentational component that renders a card-like item in the UI.**

Is used to indicate that if clicked a user will navigate to a new view.

### Props

<PropsTable of={Card} />