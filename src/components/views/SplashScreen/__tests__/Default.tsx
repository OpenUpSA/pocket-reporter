/* eslint-disable no-console  */

import React from 'react';
import SplashScreen from '../index';
import mockProps from '../schema';

const Default = (): JSX.Element => <SplashScreen {...mockProps} />;

export default Default;
