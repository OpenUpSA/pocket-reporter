/* eslint-disable no-console  */

import React from 'react';
import Card from '../index';
import mockProps from '../schema';

const Default = (): JSX.Element => <Card {...mockProps} />;

export default Default;
